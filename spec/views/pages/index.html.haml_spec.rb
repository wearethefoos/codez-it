require 'spec_helper'

describe "posts/index" do
  # subject { render; rendered }
  # before { @pages = assign(:pages, FactoryGirl.create_list(:page, 2)) }

  # it {
  #   should have_selector "table", count: 1
  #   should have_selector "tr>td",
  #     :text => @pages.first.title, :count => 1
  #   should have_selector "tr>td",
  #     :text => @pages.last.title, :count => 1
  # }
end
